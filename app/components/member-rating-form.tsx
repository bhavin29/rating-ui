'use client';

import { Card } from '@/app/components/ui';

export function MemberRatingForm() {
  return (
    <Card className="text-sm text-slate-600">
      The live GraphQL schema does not currently expose the teammate list and role-based questions needed to render
      this form.
    </Card>
  );
}
